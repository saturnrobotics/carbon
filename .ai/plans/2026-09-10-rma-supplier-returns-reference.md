# RMA and supplier returns reference documentation

Add grounded reference coverage for both directions of the returns workflow on the
`returns-module` branch. Keep the pages aligned with the shipped UI, database schema,
and computed lifecycle rules.

## Tasks

- [x] Research the documentation IA and both return workflows from source.
- [x] Add tested status-color registry entries for sales and purchase return orders.
- [x] Author `rmas.mdx` and `supplier-returns.mdx` with fields, lifecycle, workflow,
      constraints, troubleshooting context, screenshots, and related links.
- [x] Register both pages in Fumadocs ordering and the grouped reference navigation.
- [x] Run docs validation, agent knowledge-base generation, scoped utility tests and
      typecheck, then commit and push all changes.
