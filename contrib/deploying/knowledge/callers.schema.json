{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/knowledge-trusted-callers-v1.json",
  "title": "Knowledge trusted caller configuration",
  "description": "Mirrors trustedCallerConfigurationSchema in packages/knowledge/src/identity.server.ts. Subject uniqueness across callers is enforced by the runtime schema and validate-callers.ts, not expressible here.",
  "$defs": {
    "boundedString": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2048,
      "pattern": "\\S"
    },
    "assurance": {
      "description": "How the caller's requests satisfy Carbon's MFA requirement. Omitted means carbon-mfa. workspace-equivalent names the IAP access level the operator has documented as equivalent to Carbon MFA; the verifier requires it in google.access_levels. There is no third mode.",
      "type": "object",
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["mode"],
          "properties": { "mode": { "const": "carbon-mfa" } }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["mode", "accessLevel"],
          "properties": {
            "mode": { "const": "workspace-equivalent" },
            "accessLevel": { "$ref": "#/$defs/boundedString" }
          }
        }
      ]
    }
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "receiver", "callers"],
  "properties": {
    "version": { "const": 1 },
    "receiver": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "audience"],
      "properties": {
        "id": { "$ref": "#/$defs/boundedString" },
        "audience": { "$ref": "#/$defs/boundedString" }
      }
    },
    "callers": {
      "type": "array",
      "minItems": 1,
      "maxItems": 100,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "callerId",
          "serviceAccountSubject",
          "sourceIapAudience",
          "operations",
          "capabilities",
          "requiredAccessLevels"
        ],
        "properties": {
          "callerId": { "$ref": "#/$defs/boundedString" },
          "serviceAccountSubject": { "$ref": "#/$defs/boundedString" },
          "sourceIapAudience": { "$ref": "#/$defs/boundedString" },
          "operations": {
            "type": "array",
            "minItems": 1,
            "maxItems": 100,
            "items": { "$ref": "#/$defs/boundedString" }
          },
          "capabilities": {
            "type": "array",
            "maxItems": 100,
            "items": { "$ref": "#/$defs/boundedString" }
          },
          "requiredAccessLevels": {
            "type": "array",
            "maxItems": 20,
            "items": { "$ref": "#/$defs/boundedString" }
          },
          "assurance": { "$ref": "#/$defs/assurance" }
        }
      }
    }
  }
}
